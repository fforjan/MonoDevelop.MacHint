MonoDevelop.MacHint is adding functionnalies in MonoDevelop to integrate into MacOsX. As example being able to used the Growl notification Mechanism.

You need to have the MonoMac plugin install into MonoDevelop to compile it.

Growl is already provided as a binary in order to simplify the management. 
You can update it by downloadding the Growl SDK